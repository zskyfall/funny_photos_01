<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ginz.funnyphoto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider android:authorities="com.facebook.app.FacebookContentProvider963694093943725"
            android:name="com.facebook.FacebookContentProvider"
            android:exported="true"/>
        <meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity android:name=".screen.profile.UpdateProfileActivity" />
        <activity android:name=".screen.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.registration.RegistrationActivity" />
        <activity android:name=".screen.post.PostDetailActivity"></activity>
        <activity android:name=".screen.main.MainActivity"/>
        <activity android:name=".screen.post.PostPhotoActivity"/>
    </application>

</manifest>